# run_report() informs when quiet = FALSE

    Code
      run_report(file = "report.qmd", quiet = FALSE)
    Message
      v Report rendered at 'report/report.html'
      i Use `archive_report()` to archive the rendered report in 'results/reports/'

