% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save.R
\name{save_outputs}
\alias{save_outputs}
\alias{save_data}
\alias{save_table}
\alias{save_figure}
\title{Save outputs in the project}
\usage{
save_data(
  data,
  name,
  overwrite = TRUE,
  quiet = getOption("lbstproj.quiet", FALSE)
)

save_table(
  table,
  name,
  export = TRUE,
  overwrite = TRUE,
  quiet = getOption("lbstproj.quiet", FALSE),
  ...
)

save_figure(
  figure,
  name,
  extension = "png",
  width = 6,
  height = 4,
  overwrite = TRUE,
  quiet = getOption("lbstproj.quiet", FALSE),
  ...
)
}
\arguments{
\item{data}{A \code{data.frame} to save.}

\item{name}{\emph{Character}. File name without extension.}

\item{overwrite}{\emph{Logical}. If \code{TRUE}, overwrite an existing file with the same
name. If \code{FALSE}, an error is thrown when the target file already exists.

\emph{Default}: \code{TRUE}. Outputs are overwritten by default.}

\item{quiet}{Logical. If \code{TRUE}, suppress informational messages.

\emph{Default}: \code{FALSE} unless the global option \code{lbstproj.quiet} is set otherwise. The default option can be changed using \code{options(lbstproj.quiet = TRUE)}}

\item{table}{A table object to save. Must be a \code{gt_tbl} for \code{gt} projects or
a \code{flextable} for \code{flextable} projects.}

\item{export}{\emph{Logical}. If \code{TRUE}, also export the table as a \code{.docx} file.

\emph{Default}: \code{TRUE}. Word tables are always produced unless specified otherwise.}

\item{...}{Additional arguments passed to the saving function:
\itemize{
\item For \code{save_table()}: passed to \code{\link[gt:gtsave]{gt::gtsave()}} (gt projects) or
\code{\link[flextable:save_as_docx]{flextable::save_as_docx()}} (flextable projects)
\item For \code{save_figure()}: passed to \link[ggplot2:ggsave]{ggplot2::ggsave}
}}

\item{figure}{A \code{ggplot} object to save.}

\item{extension}{\emph{Character}. Output format for figures: \code{"png"} or \code{"pdf"}.

\emph{Default}: \code{"png"}}

\item{width, height}{\emph{Numeric}. Width and height of the saved figure in inches.

\emph{Default}: A width of 6 and a height of 4. This fits nicely on a portrait A4 page.}
}
\value{
Invisibly returns the path of the saved file.
}
\description{
These functions standardize how outputs are saved in an
\code{lbstproj} project:
\itemize{
\item \code{save_data()} saves a data frame as \code{rds} to \code{data/processed}.
\item \code{save_table()} saves a table as \code{rds} to \code{data/tables} and optionally
exports it as \code{docx} to \code{results/tables}. The accepted table class and
the export method depend on the project's table engine (\code{"gt"} or
\code{"flextable"}), stored in the \code{TableEngine} field of the project
\code{DESCRIPTION} file. Legacy projects without that field default to \code{"gt"}.
\item \code{save_figure()} saves a figure as \code{png} (or another extension) to \code{results/figures}.
}

To save another R object (e.g. a model, a \code{mice} object, ...) to file, use
\link[base:readRDS]{base::saveRDS}. By convention, save it to \verb{data/<subdir>/} where \verb{<subdir>}
matches the subdirectory in \verb{R/} where the script lives. For example, a model
created in \verb{R/models/} should be saved to \verb{data/models/}. When using
\code{\link[=create_file]{create_file()}} with a custom type, the matching \verb{data/<subdir>/} directory
is created automatically.

Each function saves the object using the given name, after validating it.
Figure and table names can only contain letters, numbers, and hyphens.
Data names may also contain underscores.
}
\examples{
with_example_project({
  # Save a data frame to data/processed/
  save_data(mtcars, name = "analysis-dataset", quiet = TRUE)

  # Save a ggplot figure to results/figures/
  fig <- ggplot2::ggplot(mtcars, ggplot2::aes(wt, mpg)) +
    ggplot2::geom_point()
  save_figure(fig, name = "mpg-vs-weight", quiet = TRUE)

  # Save a gt table to data/tables/ (export = FALSE skips the .docx export)
  tbl <- gt::gt(head(mtcars))
  save_table(tbl, name = "summary", export = FALSE, quiet = TRUE)

  fs::dir_tree(c("data/processed", "data/tables", "results/figures"))
})

}
