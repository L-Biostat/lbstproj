% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/create_from_tot.R
\name{create_from_tot}
\alias{create_from_tot}
\title{Create all R scripts listed in the table of tables (TOT)}
\usage{
create_from_tot(dry_run = TRUE, quiet = getOption("lbstproj.quiet", FALSE))
}
\arguments{
\item{dry_run}{\emph{Logical}. If \code{TRUE}, no files are created and the function only
reports what would be generated.

\emph{Default}: \code{TRUE} to avoid generate wrong scripts by mistake.}

\item{quiet}{Logical. If \code{TRUE}, suppress informational messages.

\emph{Default}: \code{FALSE} unless the global option \code{lbstproj.quiet} is set otherwise. The default option can be changed}
}
\value{
Invisibly returns \code{NULL}. The function is called for its side effects
(file creation and CLI reporting).
}
\description{
This function loads the TOT and creates all the R scripts for figures and
tables listed there in their respective directories in the \code{R} folder. Any
existing scripts with the same names will NOT be overwritten.
}
\examples{
with_example_project({
  # Dry run (default): reports what would be created without writing files
  create_from_tot()

  # Actually create the scripts
  create_from_tot(dry_run = FALSE, quiet = TRUE)
  fs::dir_tree("R")
}, with_tot = TRUE)

}
