% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_all_files.R
\name{run_all_files}
\alias{run_all_files}
\alias{run_all_figures}
\alias{run_all_tables}
\title{Run R Scripts in a Project Subdirectory}
\usage{
run_all_files(type, glob = "*.R", quiet = getOption("lbstproj.quiet", FALSE))

run_all_figures(glob = "*.R", quiet = getOption("lbstproj.quiet", FALSE))

run_all_tables(glob = "*.R", quiet = getOption("lbstproj.quiet", FALSE))
}
\arguments{
\item{type}{\emph{Character}. The type of scripts to run. Controls the
subdirectory of \verb{R/} that is searched:
\itemize{
\item \code{"figure"} → \verb{R/figures/}
\item \code{"table"} → \verb{R/tables/}
\item \code{"data"} → \verb{R/data/}
\item \code{"analysis"} → \verb{R/analysis/}
\item any other value → \verb{R/<type>s/}
}}

\item{glob}{\emph{Character}. A glob pattern passed to \code{\link[fs:dir_ls]{fs::dir_ls()}} to include
or exclude files.

\emph{Default}: \code{"*.R"} — matches all R scripts (case-sensitive).}

\item{quiet}{\emph{Logical}. If \code{TRUE}, suppress informational messages.

\emph{Default}: \code{FALSE} unless the global option \code{lbstproj.quiet} is set
otherwise. The default can be changed via \code{options(lbstproj.quiet = TRUE)}.}
}
\value{
Invisibly returns the character vector of sourced file paths.
}
\description{
\code{run_all_files()} sources all R scripts matching a glob pattern inside a
subdirectory of the project's \verb{R/} folder. The target subdirectory is derived
from the \code{type} argument using the same convention as \code{\link[=create_file]{create_file()}}:
\code{"figure"} maps to \verb{R/figures/}, \code{"table"} maps to \verb{R/tables/},
\code{"data"} maps to \verb{R/data/}, \code{"analysis"} maps to \verb{R/analysis/}, and any
other value maps to \verb{R/<type>s/}.

For \code{type} values \code{"figure"} or \code{"table"}, the function additionally checks
for discrepancies between the files in the directory and the table of tables
(ToT) before sourcing, and issues a warning if any are found.

\code{run_all_figures()} and \code{run_all_tables()} are convenient wrappers around
\code{run_all_files()}.
}
\section{Functions}{
\itemize{
\item \code{run_all_figures()}: Run all R scripts in \code{R/figures}

\item \code{run_all_tables()}: Run all R scripts in \code{R/tables}

}}
\examples{
\dontrun{
run_all_files("figure")
run_all_figures()                                 # equivalent to the above
run_all_files("data")
run_all_files("figure", glob = "fig-[0-9]*.R")   # only numbered figures
run_all_files("figure", quiet = TRUE)             # suppress all messages
}

}
