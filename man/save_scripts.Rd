% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save.R
\name{save_scripts}
\alias{save_scripts}
\alias{save_data}
\alias{save_figure}
\alias{save_table}
\title{Save analysis objects as \code{.rds}}
\usage{
save_data(object, name, overwrite = FALSE, print = NULL, ...)

save_figure(object, name, overwrite = FALSE, print = NULL, ...)

save_table(object, name, overwrite = FALSE, print = NULL, ...)
}
\arguments{
\item{object}{The object to save (figure/table/data). Any R object supported
by \code{\link[base:readRDS]{base::saveRDS()}}.}

\item{name}{Name used to save the object (without file extension).}

\item{overwrite}{Whether to overwrite an existing file with the same name. Defaults to \code{FALSE}.}

\item{print}{Whether to print (default: \code{TRUE}) a success message to the console. You can set a default print behavior for all functions in this family by setting the global option \code{save.print} to \code{TRUE} or \code{FALSE}.}

\item{...}{Additional arguments passed to \code{\link[base:readRDS]{base::saveRDS()}} (e.g.,
\code{compress}, \code{version}).}
}
\description{
A family of helpers that save figures, tables, and data sets as
\strong{\code{.rds}} files via \code{\link[base:readRDS]{base::saveRDS()}}. Each function writes to its
designated output directory.
}
\details{
These helpers are called for their side effects and do not return a value.
}
\section{Functions}{
\itemize{
\item \code{save_data()}: Save a data object to \verb{data/processed/}.

\item \code{save_figure()}: Save a figure to \verb{data/figures/}.

\item \code{save_table()}: Save a table to \verb{data/tables/}.

}}
\examples{
\dontrun{
# Figures
save_figure(plot, "main_effect", compress = "xz")

# Tables
save_table(tbl, "baseline_summary")

# Data (saved to data/processed/)
save_data(df, "patients_clean", version = 3)
}

}
\seealso{
\code{\link[base:readRDS]{base::saveRDS()}}
}
