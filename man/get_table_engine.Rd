% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{get_table_engine}
\alias{get_table_engine}
\alias{is_gt_project}
\alias{is_flextable_project}
\title{Table engine helpers}
\usage{
get_table_engine()

is_gt_project()

is_flextable_project()
}
\value{
\code{get_table_engine()} returns a single character string: \code{"gt"} or
\code{"flextable"}.

\code{is_gt_project()} and \code{is_flextable_project()} return a single logical.
}
\description{
\code{get_table_engine()} reads the \code{TableEngine} field from the active project's
\code{DESCRIPTION} file and returns its value. If the field is absent (e.g. in
legacy projects), it returns \code{"gt"} silently for backwards compatibility.

\code{is_gt_project()} returns \code{TRUE} if the project's \code{TableEngine} is \code{"gt"}.

\code{is_flextable_project()} returns \code{TRUE} if the project's \code{TableEngine} is
\code{"flextable"}.
}
\examples{
if (FALSE) {
  get_table_engine()
  is_gt_project()
  is_flextable_project()
}
}
\seealso{
\code{\link[=is_gt_project]{is_gt_project()}}, \code{\link[=is_flextable_project]{is_flextable_project()}}
}
