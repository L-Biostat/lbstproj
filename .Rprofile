if (interactive()) {
  suppressMessages(require(devtools))
}
options(
  usethis.description = list(
    "Authors@R" = utils::person(
      "Alexandre",
      "Bohyn",
      email = "alexandre.bohyn@kuleuven.be",
      role = c("aut", "cre"),
      comment = c(ORCID = "0000-0001-9776-7467")
    )
  )
)
