# Create the standard project structure

`create_structure()` creates the standard folder structure for the
project. Any folders that already exist will be skipped.

## Usage

``` r
create_structure(quiet = FALSE)
```
