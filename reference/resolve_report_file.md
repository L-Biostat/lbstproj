# Resolve a report file path from an explicit file name or the latest match.

Resolve a report file path from an explicit file name or the latest
match.

## Usage

``` r
resolve_report_file(file = NULL, extensions, action = "use")
```

## Value

*Character*. The path of the report file, relative to the project root.
