# Internal function to save an R object to the appropriate directory

Internal function to save an R object to the appropriate directory

## Usage

``` r
save_object(type, object, name, overwrite, print = NULL, ...)
```
